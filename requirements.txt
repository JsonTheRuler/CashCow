# Cash Cow — dashboard + API
# Prefer: python -m pip install -r requirements.txt  (same interpreter as `streamlit run`)
streamlit>=1.40.0
streamlit-option-menu>=0.3.13
streamlit-autorefresh>=1.0.1
pandas>=2.0.0
requests>=2.32.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
pydantic>=2.0.0
